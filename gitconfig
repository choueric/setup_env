[user]
	name = choueric
	email = zhssmail@gmail.com
[push]
	default = simple
[core]
	quotepath = false
	editor = vim
	# sudo npm install -g diff-so-fancy
	pager = diff-so-fancy | less --tabs=4 -RFX
[color]
	ui = auto
[color "diff"]
	meta = yellow
	frag = cyan reverse bold
	old = red bold
	new = green bold
[alias]
	st = status -s
	co = checkout
	ll = log --graph --pretty=format:'%C(yellow)%h%Creset%C(red)%d%Creset %s %C(bold blue)<%an>%Creset %Cgreen(%cr)' --abbrev-commit
	la = log --oneline --decorate --graph --all --color=always
	br = branch -vv
	diff = diff --check
	extdiff = difftool
[log]
	follow = true
# [http]
# 	proxy = socks5://127.0.0.1:1080
# [https]
# 	proxy = socks5://127.0.0.1:1080
# [sendemail]
# 	from = Haishan Zhou <zhssmail@gmail.com>
# 	smtpuser = zhssmail@gmail.com
# 	smtpserver = smtp.gmail.com
# 	smtpserverport = 465
# 	smtpencryption = ssl
# 	chainreplyto = false
# 	smtppass = xxx
# 	suppresscc = self
[diff]
	tool = meld
